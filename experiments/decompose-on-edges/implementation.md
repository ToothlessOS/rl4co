In this experiment, we want to explore whether the $\alpha$-nearest metric for edges introduced in LKH could be helpful for decomposing a larger TSP instance into smaller subproblems.

(Finished)
Specifically, in the first stage, we will implement:

1. A TSP instance generator based on the API provided by `rl4co`
2. Use farthest insertion and LKH (binary provided in the LKH-2.0.11 directory) to solve the TSP instances.
3. Compute the $\alpha$-nearast distance for the solutions generated by farthest insertion.
4. Plot the two results side-by-side, with edge colors indicating the $\alpha$-nearest distance.

(Current)
In the second stage, we will include the edge scores by `NeuroLKH` as a new metric:

1. Adapt the `NeuroLKH` inference pipeline to generate edge scores
2. Add a new subplot in visulization to visualizie the edge socres